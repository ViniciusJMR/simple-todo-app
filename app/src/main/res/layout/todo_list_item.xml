<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools">
    
    <data>
        <variable
                name="todoItem"
                type="dev.vinicius.todoapp.domain.dto.TodoItemDTOOutput" />

        <variable
                name="todoItemViewHolder"
                type="dev.vinicius.todoapp.ui.adapter.TodoItemAdapter.ViewHolder" />
    </data>

    <com.google.android.material.card.MaterialCardView
            android:layout_width="match_parent"
            android:layout_height="100dp"
            android:layout_marginTop="8dp"
            android:layout_marginEnd="8dp"
            android:layout_marginStart="8dp"
            android:clickable="true"
            android:focusable="true"
            app:cardCornerRadius="16dp">

        <androidx.constraintlayout.widget.ConstraintLayout
                android:id="@+id/cl_todo_item"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:layout_margin="8dp">

            <TextView
                    android:id="@+id/tv_todo_item_title"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="8dp"
                    android:layout_marginStart="8dp"
                    android:text="@{todoItem.name}"
                    app:layout_constraintTop_toTopOf="@+id/cl_todo_item"
                    app:layout_constraintStart_toStartOf="@+id/cl_todo_item"
                    tools:text="Titulo"
                    />

            <TextView
                    android:id="@+id/tv_todo_item_creation_date"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="8dp"
                    android:layout_marginEnd="8dp"
                    android:text="@{todoItem.formatedDate}"
                    app:layout_constraintTop_toTopOf="@+id/cl_todo_item"
                    app:layout_constraintEnd_toEndOf="@+id/cl_todo_item"
                    tools:text="27/08"/>

        </androidx.constraintlayout.widget.ConstraintLayout>
    </com.google.android.material.card.MaterialCardView>
</layout>
